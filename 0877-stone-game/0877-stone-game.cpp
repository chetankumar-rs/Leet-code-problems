class Solution {
public:
    bool stoneGame(vector<int>& piles) {
        // int n = piles.size();
        // int res = 0;
        // for(int i = 0;i < n;i++)
        // {
        //     res ^= piles[i];
        // }

        // return res;
        return true;
    }
};